export function V2Footer() {
  return (
    <footer className="rounded-md border border-dashed border-border bg-muted/30 p-3 text-xs text-muted-foreground">
      <strong className="text-foreground">v2 roadmap:</strong> performance tracking (mention-rate
      delta over time), team collaboration, CMS publishing (HubSpot/WordPress), legal review gate,
      voice morning brief (Telli), public widget. Сьогодні ship&apos;имо radar + simulator + multi-channel
      expand + Slack brief.
    </footer>
  );
}
